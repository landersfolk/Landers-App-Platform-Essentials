# image tag + SPRING_PROFILES_ACTIVE are placeholders, rendered per-environment by
# deploy/09-deploy-services.sh from envs/<env>.env (IMAGE_TAG/ENVIRONMENT). Do not
# kubectl apply this file directly.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: admin-service
  template:
    metadata:
      labels:
        app: admin-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "7071"
        prometheus.io/path: "/actuator/prometheus"
    spec:
      containers:
      - name: admin-service
        image: localhost:5000/admin-service:__IMAGE_TAG__
        imagePullPolicy: Always
        ports:
        - containerPort: 7071
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: "__ENVIRONMENT__"
        - name: VAULT_HOST
          value: "vault-prod"
        - name: VAULT_PORT
          value: "8200"
        - name: VAULT_SCHEME
          value: "http"
        - name: VAULT_AUTH_METHOD
          value: "APPROLE"
        - name: VAULT_ROLE_ID
          valueFrom:
            secretKeyRef:
              name: admin-service-vault-creds
              key: role_id
        - name: VAULT_SECRET_ID
          valueFrom:
            secretKeyRef:
              name: admin-service-vault-creds
              key: secret_id
---
apiVersion: v1
kind: Service
metadata:
  name: admin-service
spec:
  selector:
    app: admin-service
  ports:
    - port: 7071
      targetPort: 7071
