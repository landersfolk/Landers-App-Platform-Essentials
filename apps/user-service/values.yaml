nameOverride: user-service

image:
  repository: ghcr.io/landersfolk/user-service
  tag: "c884f14"
  pullPolicy: Always

containerPort: 7075

service:
  port: 7075

env:
  SPRING_PROFILES_ACTIVE: qa

# dev cluster (k3d-landers-app): TOKEN auth with the shared root token,
# matching every service's own application.yml default and
# vault-seed-secrets.sh. QA/prod overrides this to authMethod: APPROLE +
# secretName: user-service-vault-creds (see vault-production/setup-approle.sh).
vault:
  enabled: true
  host: vault-server
  port: "8200"
  scheme: http
  authMethod: TOKEN
  token: root

prometheus:
  enabled: true
  path: /actuator/prometheus
