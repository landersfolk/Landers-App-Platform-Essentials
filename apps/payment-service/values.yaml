nameOverride: payment-service

image:
  repository: ghcr.io/landersfolk/payment-service
  tag: "aac9eae"
  pullPolicy: Always

containerPort: 7077

service:
  port: 7077

env:
  SPRING_PROFILES_ACTIVE: qa

# QA (EC2, this branch): real per-service AppRole identity, matching
# vault-production/setup-approle.sh. role_id/secret_id come from the
# payment-service-vault-creds k8s Secret (created by
# deploy/09-deploy-services.sh's Vault AppRole bootstrap step, not by
# this chart).
vault:
  enabled: true
  host: vault-prod
  port: "8200"
  scheme: http
  authMethod: APPROLE
  secretName: payment-service-vault-creds

prometheus:
  enabled: true
  path: /actuator/prometheus
